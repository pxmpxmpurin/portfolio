---
const { title, description, github, demo } = Astro.props;
---

<article class="p-6 border rounded-xl shadow-sm dark:border-zinc-700 bg-white dark:bg-zinc-900">
  <h2 class="text-xl font-semibold mb-2">{title}</h2>
  <p class="text-gray-600 dark:text-gray-300 mb-4">{description}</p>
  <div class="flex gap-4">
    {github && (
      <a href={github} target="_blank" class="text-sm text-blue-500 hover:underline">
        Código
      </a>
    )}
    {demo && (
      <a href={demo} target="_blank" class="text-sm text-blue-500 hover:underline">
        Demo
      </a>
    )}
  </div>
</article>
