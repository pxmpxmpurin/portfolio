---
const { title, description, github, demo, image } = Astro.props;
---

<article class="flex flex-col md:flex-row gap-4 p-4 border rounded-xl shadow-sm dark:border-zinc-700 bg-white dark:bg-zinc-900 hover:shadow-md transition">
  {image && (
    <img
      src={image}
      alt={title}
      class="w-full md:w-40 h-40 object-cover rounded-md"
    />
  )}
  <div class="flex flex-col justify-between">
    <div>
      <h2 class="text-xl font-semibold mb-2">{title}</h2>
      <p class="text-gray-600 dark:text-gray-300 mb-4">{description}</p>
    </div>
    <div class="flex gap-4">
      {github && (
        <a href={github} target="_blank" class="text-sm text-blue-500 hover:underline">
          Código
        </a>
      )}
      {demo && (
        <a href={demo} target="_blank" class="text-sm text-blue-500 hover:underline">
          Demo
        </a>
      )}
    </div>
  </div>
</article>
